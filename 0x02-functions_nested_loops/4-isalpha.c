#include "holberton.h"
/**
 * _isalpha - confirm if return output is a alphabetic char
 * @c: variable used
 * Return: 0
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (0);
}
