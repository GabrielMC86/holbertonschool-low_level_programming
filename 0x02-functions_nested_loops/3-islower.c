#include "holberton.h"
/**
 * _islower - confirm if character is lowercase
 * @c: type var used
 * Return: 0
 */
int _islower(int c)
{

	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (0);
}
