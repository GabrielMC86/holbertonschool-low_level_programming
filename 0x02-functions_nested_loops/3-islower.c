#include "holberton.h"
/**
 * main - confirm if character is lowercase
 * int: type var used
 * Return: 0
 */
int _islower(int c)
{
		
	if (c == 97 && c < 123)
	{
		return (1);
	}
	else 
	{
		return (0);
	}
return (0);
}
